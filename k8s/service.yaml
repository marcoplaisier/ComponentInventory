apiVersion: v1
kind: Service
metadata:
  name: component-inventory
  namespace: component-inventory
  labels:
    app: component-inventory
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: component-inventory
